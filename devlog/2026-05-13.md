# 开发日志 - 2026-05-13

## 已完成

- [x] 创建项目工程配置（package.json、TypeScript、Vite、Tailwind）
- [x] 安装所有依赖（React、Ant Design、ECharts、sql.js、Express 等）
- [x] 创建 React 前端基础代码（App.tsx、主题、全局样式）
- [x] 创建 Express 本地服务器（server/index.js）
- [x] 配置 Vite 构建（生产模式输出到 dist/renderer）
- [x] 实现数据库模块（server/database.js + queries.js）
  - sql.js WebAssembly SQLite，无需编译
  - 5张核心表：districts、projects、buildings、units、price_snapshots
  - 预置禅城/南海/顺德/三水/高明五个区域
- [x] 侦查目标网站 API 结构
  - 网站是服务端渲染 HTML（非 JSON API）
  - 区域 URL：/loupan/s/c4/（禅城）、c7（南海）、c10（顺德）、c13（三水）、c19（高明）
  - 项目详情：/loupan/{id}.html
  - 数据通过 house_popup() 函数参数内嵌在 onclick 中
  - 字段：房号、户型、面积、总价、单价、状态等
  - 已售/已预定房源价格显示为"-"（隐私保护）
- [x] 实现数据采集模块（server/scraper.js）
  - HTML 正则解析（项目列表 + 楼栋 + 房源）
  - 请求频率控制（500ms 间隔）
  - 区域间隔 5 秒，项目间隔 2 秒
  - 自动计算 9 折后单价
  - 增量更新（UPSERT）
- [x] 实现服务器 API 路由
  - GET /api/districts - 区域列表
  - GET /api/projects?district=X - 区域项目（按单价降序）
  - GET /api/projects/search?keyword=X - 搜索
  - GET /api/projects/:id - 项目详情+楼栋
  - GET /api/projects/:id/units - 房源列表（含价格）
  - GET /api/units/:id/history - 房源价格历史
  - GET /api/projects/:id/price-history - 项目均价走势
  - POST /api/scrape - 触发采集
- [x] 实现前端页面
  - Dashboard（五区卡片 + 统计数字）
  - DistrictView（项目列表表格，按单价降序，分页）
  - ProjectDetail（楼栋切换 + 房源表格 + 价格 + 状态标签）
  - SearchResult（搜索页）
  - PriceHistory（ECharts 折线图，单价+9折后单价）
- [x] 集成 Ant Design 淡蓝色主题 + 中文字体

## 架构变更

- 从 Electron 切换到本地 Web 服务器方案（Express + 浏览器）
- Node.js 版本：v24.14.0（主机）、v20.18.0（Electron 内部）

## 当前项目结构

```
foshan-viewer/
├── server/
│   ├── index.js          # Express 服务器 + API 路由
│   ├── database.js       # sql.js SQLite 初始化
│   ├── queries.js        # 数据库查询层
│   └── scraper.js        # 网页数据采集
├── src/renderer/
│   ├── App.tsx           # 主布局（侧边栏+内容区）
│   ├── pages/
│   │   ├── Dashboard.tsx
│   │   ├── DistrictView.tsx
│   │   ├── ProjectDetail.tsx
│   │   ├── SearchResult.tsx
│   │   └── PriceHistory.tsx
│   ├── services/api.ts   # API 调用封装
│   ├── store/useAppStore.ts  # Zustand 状态管理
│   └── styles/
│       ├── theme.ts      # Ant Design 淡蓝主题
│       └── global.css    # 全局样式
└── docs/                 # 项目文档
└── devlog/               # 开发日志
```

## 验证结果

- 服务器启动 → API 正常响应
- 区域列表 API 返回 5 个区域
- 统计 API 返回 0 项目（数据库为空）
- 前端页面加载正常

## 待办事项

- [ ] 首次数据采集测试（端到端）
- [ ] 实现 Supabase 云端同步
- [ ] 实现定时采集调度（node-cron）
- [ ] UI 打磨（加载骨架屏、空状态优化）
- [ ] 打包分发（pkg/nexe 生成 .exe）

## 备注

- 网站对已售/已预定房源隐藏价格（显示"-"），这是政府隐私保护
- 采集需要登录吗？目前看到页面无需登录即可访问
- 后续可添加数据导出为 CSV/Excel 功能